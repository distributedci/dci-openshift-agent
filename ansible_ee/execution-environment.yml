---
version: 3

images:
  base_image:
    name: ghcr.io/ansible/creator-ee:latest

options:
    package_manager_path: /usr/bin/microdnf

dependencies:
  galaxy: requirements.yml
  system: bindep.txt
...
