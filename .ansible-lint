skip_list:
  - '301'                       # we use a lot of shell commands to get info
  - '303'                       # sometimes the module doesn't cover all the command options
  - '602'                       # For readability, we want to ensure a variable is empty, also there's no concensus on what's a best practice: https://github.com/ansible-community/ansible-lint/issues/457
