---
- name: Get CSR's to approve
  command:
    cmd: "{{ lookup('first_found', findme) }} {{ ocp_binary_path }} {{ groups['workers'] | length }} {{ dir }}/auth/kubeconfig"
  vars:
    findme:
      - approve_workers.sh
...
