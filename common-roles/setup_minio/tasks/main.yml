---
- name: Deploy a Minio S3 storage provider
  block:
    - name: Pre-run validations
      include_tasks: validation.yml

    - name: Create Minio deployment
      include_tasks: setup-minio.yml
  when:
    - sm_action == 'install'

- name: "Cleanup Logging stack resources"
  include_tasks: clean-resources.yml
  when:
    - sm_action == 'cleanup'
...
