---
- name: "mirror-images: Mirror images"
  include_task:
    name: mirror-images.yml
  loop: "{{ images }}"
  loop_control:
    loop_var: image
  when:
    - images is defined
    - images | length
