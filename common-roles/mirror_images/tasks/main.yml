---
- name: "mirror_images : Mirror images"
  include_tasks: mirror-images.yml
  loop: "{{ mi_images }}"
  loop_control:
    loop_var: image
  when:
    - mi_images | default([]) | length
    - mi_local_registry | default("") | length
