---
serverport: 8000
timeout: 600
webserver_url: ""
cache_dir: "/opt/cache"
pullsecret_file: "{{ cache_dir }}/pull-secret.txt"
provision_cache_store: "{{ cache_dir }}"
upi_user_id: "{{ ansible_user_id }}"
upi_group_id: "{{ ansible_user_id }}"
...
