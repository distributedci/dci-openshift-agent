---
- name: "Check if OpenShift client tools have been unpacked"
  stat:
    path: "{{ dir }}/oc"
    get_checksum: false
  register: unpacked
  when:
    - not mor_force  # we don't care to stat files if we're forcing

- name: "Get client tools tarball"
  block:
    - name: "Unarchive client tarball"
      unarchive:
        src: "{{ dir }}/openshift-client-linux-{{ mor_version }}.tar.gz"
        dest: "{{ dir }}"
        owner: "{{ mor_owner }}"
        group: "{{ mor_group }}"
        mode: "0755"
        remote_src: true
        exclude:
          - README.md

    - name: "Marker for doaa to avoid redownloaded"
      file:
        state: touch
        dest: "{{ dir }}/client_tools.done"
        owner: "{{ mor_owner }}"
        group: "{{ mor_group }}"
        mode: "0644"
  become: true
  when:
    - mor_force or not unpacked.stat.exists

- name: "Process extra artifacts"
  block:
    - name: "Check if extra mor_artifacts have already been unpacked"
      stat:
        path: "{{ dir }}/mor_artifacts.done"
        get_checksum: false
      register: unpacked
      when:
        - not mor_force  # we don't care to stat files if we're forcing

    - name: "Unarchive extra artifact tarballs"
      unarchive:
        src: "{{ dir }}/{{ item }}"
        dest: "{{ dir }}"
        owner: "{{ mor_owner }}"
        group: "{{ mor_group }}"
        mode: "0755"
        remote_src: true
        exclude:
          - README.md
      become: true
      loop: "{{ mor_artifacts }}"
      when:
        - mor_force or not unpacked.stat.exists

    - name: "Touch marker for extra mor_artifacts unpacked"
      file:
        dest: "{{ dir }}/mor_artifacts.done"
        state: touch
        owner: "{{ mor_owner }}"
        group: "{{ mor_group }}"
        mode: "0644"
      become: true
      when:
        - mor_force or not unpacked.stat.exists
  when:
    - mor_mirror_artifacts | bool
    - mor_unpack_artifacts | bool
...
