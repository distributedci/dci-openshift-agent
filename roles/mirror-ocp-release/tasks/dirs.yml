---
- name: "Create master cache directory"
  file:
    path: "{{ cache_dir }}"
    state: directory
    owner: "{{ owner | int }}"
    group: "{{ group | int }}"
    mode: "0775"
  become: true

- name: "Create cache directory for release"
  file:
    path: "{{ cache_dir }}/{{ version }}"
    owner: "{{ owner | int }}"
    group: "{{ group | int }}"
    mode: "0775"
    state: directory
  become: true
...
