---
- name: Apply ClusterInstance templates to create spoke
  vars:
    clusterinstance_namespace: "{{ cluster_name }}"
  kubernetes.core.k8s:
    state: present
    template: "{{ item }}"
    wait: true
  with_fileglob:
    - "{{ dci_clusterinstance_template_dir }}/{{ cluster_name }}-*.j2"
